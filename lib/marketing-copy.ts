/**
 * One signup label everywhere — avoids mixing “Get started” and “Sign up”.
 */
export const marketingCta = {
  signUp: "Sign up",
  /** Distinct from hero primary CTA — both land in-console; avoids repeating the same label. */
  connectExplore: "Go to hub",
} as const;
